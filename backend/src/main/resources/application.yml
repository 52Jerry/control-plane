server:
  port: ${CONTROL_PLANE_PORT:8090}

spring:
  application:
    name: node-control-plane
  datasource:
    url: jdbc:mysql://rm-bp1gq2po14z29s0kxuo.mysql.rds.aliyuncs.com:3306/control-plane?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Ycp20020414@
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  jackson:
    default-property-inclusion: non_null

management:
  endpoints:
    web:
      exposure:
        include: health,info

control-plane:
  heartbeat:
    interval-ms: ${HEARTBEAT_INTERVAL_MS:15000}
  bootstrap:
    enabled: ${NODE_MANAGER_BOOTSTRAP_ENABLED:true}
    name: ${NODE_MANAGER_NAME:Vultr Node}
    base-url: ${NODE_MANAGER_BASE_URL:http://198.13.46.231:8088}
    token: ${NODE_MANAGER_TOKEN:}
  security:
    admin-token: ${CONTROL_PLANE_ADMIN_TOKEN:}

